# RFC 5545 mandates CRLF line endings for iCalendar files. Google Calendar's
# manual import rejects LF-only files ("0 of 0 events imported"). Force CRLF
# on checkout/commit regardless of host OS.
*.ics text eol=crlf
